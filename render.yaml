services:
  - type: web
    name: jobmind-match
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    autoDeploy: true
    disk:
      name: jobmind-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: APP_ENV
        value: prod
      - key: DATABASE_URL
        value: sqlite:////var/data/jobmind.db
      - key: FREE_MONTHLY_MATCH_LIMIT
        value: "50"
      - key: FREE_SOURCES
        value: remotive,weworkremotely
      - key: PYTHON_VERSION
        value: "3.11.9"
