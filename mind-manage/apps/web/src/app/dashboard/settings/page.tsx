export default function SettingsPage() {
  return (
    <main className="shell">
      <section className="panel">
        <div className="kicker">Settings</div>
        <h1>Providers and workspace configuration</h1>
        <p>Configure Google Maps, OpenAI, SMTP, n8n, and role-based access rules for the sales team.</p>
      </section>
    </main>
  );
}
